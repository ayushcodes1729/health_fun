.anchor
.DS_Store
target
**/*.rs.bk
node_modules
test-ledger
.yarn
to-do.md
# iCloud sync conflict copies, in both forms it produces: a duplicated file
# ("attestation 2.ts") and a duplicated directory ("accounts 2/"). The space
# before the digit is what makes this safe; neither matches a legitimate name
# like "sha256.ts".
* [0-9].*
* [0-9]/
