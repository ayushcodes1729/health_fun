/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { PublicKey } from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  i64,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type AttestationData = {
  challengeId: bigint;
  user: PublicKey;
  steps: number;
  sleepHours: number;
  gym: boolean;
  /**
   * Whole days since the Unix epoch, i.e. `unix_timestamp / 86400`.
   * The oracle MUST use this numbering: it is compared directly against
   * `stake_account.last_day_checked`, which the program derives from the
   * chain clock. Any other scheme (days since challenge start, a calendar
   * ordinal) silently stops goal progress from ever accruing.
   */
  epochDay: number;
  nonce: bigint;
  expiresAt: bigint;
};

export type AttestationDataArgs = {
  challengeId: number | bigint;
  user: PublicKey;
  steps: number;
  sleepHours: number;
  gym: boolean;
  /**
   * Whole days since the Unix epoch, i.e. `unix_timestamp / 86400`.
   * The oracle MUST use this numbering: it is compared directly against
   * `stake_account.last_day_checked`, which the program derives from the
   * chain clock. Any other scheme (days since challenge start, a calendar
   * ordinal) silently stops goal progress from ever accruing.
   */
  epochDay: number;
  nonce: number | bigint;
  expiresAt: number | bigint;
};

export function getAttestationDataSerializer(): Serializer<
  AttestationDataArgs,
  AttestationData
> {
  return struct<AttestationData>(
    [
      ['challengeId', u64()],
      ['user', publicKeySerializer()],
      ['steps', u32()],
      ['sleepHours', u8()],
      ['gym', bool()],
      ['epochDay', u16()],
      ['nonce', u64()],
      ['expiresAt', i64()],
    ],
    { description: 'AttestationData' }
  ) as Serializer<AttestationDataArgs, AttestationData>;
}
