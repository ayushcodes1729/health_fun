/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import {
  AttestationData,
  AttestationDataArgs,
  getAttestationDataSerializer,
} from '../types';

// Accounts.
export type UpdateHealthDataInstructionAccounts = {
  user: Signer;
  healthData?: PublicKey | Pda;
  stakeConfig?: PublicKey | Pda;
  stakeAccount?: PublicKey | Pda;
  /** at runtime by comparing its address to `sysvar::instructions::id()`. */
  instructions: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type UpdateHealthDataInstructionData = {
  discriminator: Uint8Array;
  attestationData: AttestationData;
};

export type UpdateHealthDataInstructionDataArgs = {
  attestationData: AttestationDataArgs;
};

export function getUpdateHealthDataInstructionDataSerializer(): Serializer<
  UpdateHealthDataInstructionDataArgs,
  UpdateHealthDataInstructionData
> {
  return mapSerializer<
    UpdateHealthDataInstructionDataArgs,
    any,
    UpdateHealthDataInstructionData
  >(
    struct<UpdateHealthDataInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['attestationData', getAttestationDataSerializer()],
      ],
      { description: 'UpdateHealthDataInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([186, 121, 150, 217, 123, 85, 223, 75]),
    })
  ) as Serializer<
    UpdateHealthDataInstructionDataArgs,
    UpdateHealthDataInstructionData
  >;
}

// Args.
export type UpdateHealthDataInstructionArgs =
  UpdateHealthDataInstructionDataArgs;

// Instruction.
export function updateHealthData(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: UpdateHealthDataInstructionAccounts & UpdateHealthDataInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'healthFun',
    'DADe3TShkZZnA8hvpMMUNmgGDe7LGwQbXZ2KDe2hTTXJ'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    healthData: {
      index: 1,
      isWritable: false as boolean,
      value: input.healthData ?? null,
    },
    stakeConfig: {
      index: 2,
      isWritable: false as boolean,
      value: input.stakeConfig ?? null,
    },
    stakeAccount: {
      index: 3,
      isWritable: false as boolean,
      value: input.stakeAccount ?? null,
    },
    instructions: {
      index: 4,
      isWritable: false as boolean,
      value: input.instructions ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdateHealthDataInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.healthData.value) {
    resolvedAccounts.healthData.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([104, 101, 97, 108, 116, 104])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.user.value)
      ),
    ]);
  }
  if (!resolvedAccounts.stakeConfig.value) {
    resolvedAccounts.stakeConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([99, 111, 110, 102, 105, 103])),
    ]);
  }
  if (!resolvedAccounts.stakeAccount.value) {
    resolvedAccounts.stakeAccount.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([115, 116, 97, 107, 101])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.user.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdateHealthDataInstructionDataSerializer().serialize(
    resolvedArgs as UpdateHealthDataInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
