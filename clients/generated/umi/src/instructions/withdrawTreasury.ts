/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type WithdrawTreasuryInstructionAccounts = {
  admin: Signer;
  stakeConfig?: PublicKey | Pda;
  treasuryConfig?: PublicKey | Pda;
  treasuryAuthority?: PublicKey | Pda;
  treasuryVault: PublicKey | Pda;
  destination: PublicKey | Pda;
  mint: PublicKey | Pda;
  tokenProgram: PublicKey | Pda;
};

// Data.
export type WithdrawTreasuryInstructionData = {
  discriminator: Uint8Array;
  amount: bigint;
};

export type WithdrawTreasuryInstructionDataArgs = { amount: number | bigint };

export function getWithdrawTreasuryInstructionDataSerializer(): Serializer<
  WithdrawTreasuryInstructionDataArgs,
  WithdrawTreasuryInstructionData
> {
  return mapSerializer<
    WithdrawTreasuryInstructionDataArgs,
    any,
    WithdrawTreasuryInstructionData
  >(
    struct<WithdrawTreasuryInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['amount', u64()],
      ],
      { description: 'WithdrawTreasuryInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([40, 63, 122, 158, 144, 216, 83, 96]),
    })
  ) as Serializer<
    WithdrawTreasuryInstructionDataArgs,
    WithdrawTreasuryInstructionData
  >;
}

// Args.
export type WithdrawTreasuryInstructionArgs =
  WithdrawTreasuryInstructionDataArgs;

// Instruction.
export function withdrawTreasury(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: WithdrawTreasuryInstructionAccounts & WithdrawTreasuryInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: false as boolean,
      value: input.admin ?? null,
    },
    stakeConfig: {
      index: 1,
      isWritable: false as boolean,
      value: input.stakeConfig ?? null,
    },
    treasuryConfig: {
      index: 2,
      isWritable: false as boolean,
      value: input.treasuryConfig ?? null,
    },
    treasuryAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.treasuryAuthority ?? null,
    },
    treasuryVault: {
      index: 4,
      isWritable: true as boolean,
      value: input.treasuryVault ?? null,
    },
    destination: {
      index: 5,
      isWritable: true as boolean,
      value: input.destination ?? null,
    },
    mint: { index: 6, isWritable: false as boolean, value: input.mint ?? null },
    tokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: WithdrawTreasuryInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.stakeConfig.value) {
    resolvedAccounts.stakeConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([99, 111, 110, 102, 105, 103])),
    ]);
  }
  if (!resolvedAccounts.treasuryConfig.value) {
    resolvedAccounts.treasuryConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          116, 114, 101, 97, 115, 117, 114, 121, 95, 99, 111, 110, 102, 105,
          103,
        ])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.mint.value)
      ),
    ]);
  }
  if (!resolvedAccounts.treasuryAuthority.value) {
    resolvedAccounts.treasuryAuthority.value = context.eddsa.findPda(
      programId,
      [
        bytes().serialize(
          new Uint8Array([
            116, 114, 101, 97, 115, 117, 114, 121, 95, 97, 117, 116, 104, 111,
            114, 105, 116, 121,
          ])
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.mint.value)
        ),
      ]
    );
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getWithdrawTreasuryInstructionDataSerializer().serialize(
    resolvedArgs as WithdrawTreasuryInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
