/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseLegacyConfigInstructionAccounts = {
  admin: Signer;
  /** seeds bind it to the config PDA, and the handler checks owner and size. */
  stakeConfig?: PublicKey | Pda;
};

// Data.
export type CloseLegacyConfigInstructionData = { discriminator: Uint8Array };

export type CloseLegacyConfigInstructionDataArgs = {};

export function getCloseLegacyConfigInstructionDataSerializer(): Serializer<
  CloseLegacyConfigInstructionDataArgs,
  CloseLegacyConfigInstructionData
> {
  return mapSerializer<
    CloseLegacyConfigInstructionDataArgs,
    any,
    CloseLegacyConfigInstructionData
  >(
    struct<CloseLegacyConfigInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'CloseLegacyConfigInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([191, 166, 87, 122, 255, 131, 74, 87]),
    })
  ) as Serializer<
    CloseLegacyConfigInstructionDataArgs,
    CloseLegacyConfigInstructionData
  >;
}

// Instruction.
export function closeLegacyConfig(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: CloseLegacyConfigInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    stakeConfig: {
      index: 1,
      isWritable: true as boolean,
      value: input.stakeConfig ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.stakeConfig.value) {
    resolvedAccounts.stakeConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([99, 111, 110, 102, 105, 103])),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseLegacyConfigInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
