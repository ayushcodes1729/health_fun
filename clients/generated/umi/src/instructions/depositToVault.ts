/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type DepositToVaultInstructionAccounts = {
  user: Signer;
  stakeAccount?: PublicKey | Pda;
  stakeConfig?: PublicKey | Pda;
  mint: PublicKey | Pda;
  vault?: PublicKey | Pda;
  userAta?: PublicKey | Pda;
  tokenProgram: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type DepositToVaultInstructionData = {
  discriminator: Uint8Array;
  stakedAmount: bigint;
};

export type DepositToVaultInstructionDataArgs = {
  stakedAmount: number | bigint;
};

export function getDepositToVaultInstructionDataSerializer(): Serializer<
  DepositToVaultInstructionDataArgs,
  DepositToVaultInstructionData
> {
  return mapSerializer<
    DepositToVaultInstructionDataArgs,
    any,
    DepositToVaultInstructionData
  >(
    struct<DepositToVaultInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['stakedAmount', u64()],
      ],
      { description: 'DepositToVaultInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([18, 62, 110, 8, 26, 106, 248, 151]),
    })
  ) as Serializer<
    DepositToVaultInstructionDataArgs,
    DepositToVaultInstructionData
  >;
}

// Args.
export type DepositToVaultInstructionArgs = DepositToVaultInstructionDataArgs;

// Instruction.
export function depositToVault(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: DepositToVaultInstructionAccounts & DepositToVaultInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'healthFun',
    'DADe3TShkZZnA8hvpMMUNmgGDe7LGwQbXZ2KDe2hTTXJ'
  );

  // Accounts.
  const resolvedAccounts = {
    user: { index: 0, isWritable: true as boolean, value: input.user ?? null },
    stakeAccount: {
      index: 1,
      isWritable: true as boolean,
      value: input.stakeAccount ?? null,
    },
    stakeConfig: {
      index: 2,
      isWritable: false as boolean,
      value: input.stakeConfig ?? null,
    },
    mint: { index: 3, isWritable: false as boolean, value: input.mint ?? null },
    vault: {
      index: 4,
      isWritable: true as boolean,
      value: input.vault ?? null,
    },
    userAta: {
      index: 5,
      isWritable: true as boolean,
      value: input.userAta ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    systemProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: DepositToVaultInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.stakeAccount.value) {
    resolvedAccounts.stakeAccount.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([115, 116, 97, 107, 101])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.user.value)
      ),
    ]);
  }
  if (!resolvedAccounts.stakeConfig.value) {
    resolvedAccounts.stakeConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([99, 111, 110, 102, 105, 103])),
    ]);
  }
  if (!resolvedAccounts.vault.value) {
    resolvedAccounts.vault.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([118, 97, 117, 108, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.user.value)
      ),
    ]);
  }
  if (!resolvedAccounts.userAta.value) {
    resolvedAccounts.userAta.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.user.value)
        ),
        bytes().serialize(
          new Uint8Array([
            6, 221, 246, 225, 215, 101, 161, 147, 217, 203, 225, 70, 206, 235,
            121, 172, 28, 180, 133, 237, 95, 91, 55, 145, 58, 140, 245, 133,
            126, 255, 0, 169,
          ])
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.mint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getDepositToVaultInstructionDataSerializer().serialize(
    resolvedArgs as DepositToVaultInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
