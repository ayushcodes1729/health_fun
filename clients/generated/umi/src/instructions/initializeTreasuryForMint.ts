/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type InitializeTreasuryForMintInstructionAccounts = {
  admin: Signer;
  stakeConfig?: PublicKey | Pda;
  treasuryConfig?: PublicKey | Pda;
  treasuryAuthority?: PublicKey | Pda;
  treasuryVault?: PublicKey | Pda;
  mint: PublicKey | Pda;
  tokenProgram: PublicKey | Pda;
  associatedTokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitializeTreasuryForMintInstructionData = {
  discriminator: Uint8Array;
};

export type InitializeTreasuryForMintInstructionDataArgs = {};

export function getInitializeTreasuryForMintInstructionDataSerializer(): Serializer<
  InitializeTreasuryForMintInstructionDataArgs,
  InitializeTreasuryForMintInstructionData
> {
  return mapSerializer<
    InitializeTreasuryForMintInstructionDataArgs,
    any,
    InitializeTreasuryForMintInstructionData
  >(
    struct<InitializeTreasuryForMintInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'InitializeTreasuryForMintInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([83, 105, 247, 74, 152, 89, 160, 222]),
    })
  ) as Serializer<
    InitializeTreasuryForMintInstructionDataArgs,
    InitializeTreasuryForMintInstructionData
  >;
}

// Instruction.
export function initializeTreasuryForMint(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: InitializeTreasuryForMintInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );

  // Accounts.
  const resolvedAccounts = {
    admin: {
      index: 0,
      isWritable: true as boolean,
      value: input.admin ?? null,
    },
    stakeConfig: {
      index: 1,
      isWritable: false as boolean,
      value: input.stakeConfig ?? null,
    },
    treasuryConfig: {
      index: 2,
      isWritable: true as boolean,
      value: input.treasuryConfig ?? null,
    },
    treasuryAuthority: {
      index: 3,
      isWritable: false as boolean,
      value: input.treasuryAuthority ?? null,
    },
    treasuryVault: {
      index: 4,
      isWritable: true as boolean,
      value: input.treasuryVault ?? null,
    },
    mint: { index: 5, isWritable: false as boolean, value: input.mint ?? null },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
    associatedTokenProgram: {
      index: 7,
      isWritable: false as boolean,
      value: input.associatedTokenProgram ?? null,
    },
    systemProgram: {
      index: 8,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.stakeConfig.value) {
    resolvedAccounts.stakeConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([99, 111, 110, 102, 105, 103])),
    ]);
  }
  if (!resolvedAccounts.treasuryConfig.value) {
    resolvedAccounts.treasuryConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([
          116, 114, 101, 97, 115, 117, 114, 121, 95, 99, 111, 110, 102, 105,
          103,
        ])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.mint.value)
      ),
    ]);
  }
  if (!resolvedAccounts.treasuryAuthority.value) {
    resolvedAccounts.treasuryAuthority.value = context.eddsa.findPda(
      programId,
      [
        bytes().serialize(
          new Uint8Array([
            116, 114, 101, 97, 115, 117, 114, 121, 95, 97, 117, 116, 104, 111,
            114, 105, 116, 121,
          ])
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.mint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.treasuryVault.value) {
    resolvedAccounts.treasuryVault.value = context.eddsa.findPda(
      context.programs.getPublicKey(
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      ),
      [
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.treasuryAuthority.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.tokenProgram.value)
        ),
        publicKeySerializer().serialize(
          expectPublicKey(resolvedAccounts.mint.value)
        ),
      ]
    );
  }
  if (!resolvedAccounts.associatedTokenProgram.value) {
    resolvedAccounts.associatedTokenProgram.value =
      context.programs.getPublicKey(
        'associatedTokenProgram',
        'ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL'
      );
    resolvedAccounts.associatedTokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data =
    getInitializeTreasuryForMintInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
