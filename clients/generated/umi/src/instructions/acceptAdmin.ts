/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type AcceptAdminInstructionAccounts = {
  newAdmin: Signer;
  stakeConfig?: PublicKey | Pda;
};

// Data.
export type AcceptAdminInstructionData = { discriminator: Uint8Array };

export type AcceptAdminInstructionDataArgs = {};

export function getAcceptAdminInstructionDataSerializer(): Serializer<
  AcceptAdminInstructionDataArgs,
  AcceptAdminInstructionData
> {
  return mapSerializer<
    AcceptAdminInstructionDataArgs,
    any,
    AcceptAdminInstructionData
  >(
    struct<AcceptAdminInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'AcceptAdminInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([112, 42, 45, 90, 116, 181, 13, 170]),
    })
  ) as Serializer<AcceptAdminInstructionDataArgs, AcceptAdminInstructionData>;
}

// Instruction.
export function acceptAdmin(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: AcceptAdminInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );

  // Accounts.
  const resolvedAccounts = {
    newAdmin: {
      index: 0,
      isWritable: false as boolean,
      value: input.newAdmin ?? null,
    },
    stakeConfig: {
      index: 1,
      isWritable: true as boolean,
      value: input.stakeConfig ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.stakeConfig.value) {
    resolvedAccounts.stakeConfig.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([99, 111, 110, 102, 105, 103])),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getAcceptAdminInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
