/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type UserProfile = Account<UserProfileAccountData>;

export type UserProfileAccountData = {
  discriminator: Uint8Array;
  user: PublicKey;
  /** Challenges settled with `days_goal_met >= total_days`. */
  challengesCompleted: number;
  /** Challenges settled short of the goal, where the stake was forfeited. */
  challengesFailed: number;
  /** Consecutive completed challenges; reset to 0 by a forfeit. */
  currentStreak: number;
  /** Highest `current_streak` ever reached. */
  longestStreak: number;
  /**
   * Lifetime total staked, in the mint's base units. Sums across mints, so
   * it is only meaningful when a single mint is in use.
   */
  totalStaked: bigint;
  bump: number;
};

export type UserProfileAccountDataArgs = {
  user: PublicKey;
  /** Challenges settled with `days_goal_met >= total_days`. */
  challengesCompleted: number;
  /** Challenges settled short of the goal, where the stake was forfeited. */
  challengesFailed: number;
  /** Consecutive completed challenges; reset to 0 by a forfeit. */
  currentStreak: number;
  /** Highest `current_streak` ever reached. */
  longestStreak: number;
  /**
   * Lifetime total staked, in the mint's base units. Sums across mints, so
   * it is only meaningful when a single mint is in use.
   */
  totalStaked: number | bigint;
  bump: number;
};

export function getUserProfileAccountDataSerializer(): Serializer<
  UserProfileAccountDataArgs,
  UserProfileAccountData
> {
  return mapSerializer<UserProfileAccountDataArgs, any, UserProfileAccountData>(
    struct<UserProfileAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['user', publicKeySerializer()],
        ['challengesCompleted', u32()],
        ['challengesFailed', u32()],
        ['currentStreak', u32()],
        ['longestStreak', u32()],
        ['totalStaked', u64()],
        ['bump', u8()],
      ],
      { description: 'UserProfileAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([32, 37, 119, 205, 179, 180, 13, 194]),
    })
  ) as Serializer<UserProfileAccountDataArgs, UserProfileAccountData>;
}

export function deserializeUserProfile(rawAccount: RpcAccount): UserProfile {
  return deserializeAccount(rawAccount, getUserProfileAccountDataSerializer());
}

export async function fetchUserProfile(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<UserProfile> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'UserProfile');
  return deserializeUserProfile(maybeAccount);
}

export async function safeFetchUserProfile(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<UserProfile | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeUserProfile(maybeAccount) : null;
}

export async function fetchAllUserProfile(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<UserProfile[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'UserProfile');
    return deserializeUserProfile(maybeAccount);
  });
}

export async function safeFetchAllUserProfile(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<UserProfile[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeUserProfile(maybeAccount as RpcAccount));
}

export function getUserProfileGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      user: PublicKey;
      challengesCompleted: number;
      challengesFailed: number;
      currentStreak: number;
      longestStreak: number;
      totalStaked: number | bigint;
      bump: number;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      user: [8, publicKeySerializer()],
      challengesCompleted: [40, u32()],
      challengesFailed: [44, u32()],
      currentStreak: [48, u32()],
      longestStreak: [52, u32()],
      totalStaked: [56, u64()],
      bump: [64, u8()],
    })
    .deserializeUsing<UserProfile>((account) => deserializeUserProfile(account))
    .whereField(
      'discriminator',
      new Uint8Array([32, 37, 119, 205, 179, 180, 13, 194])
    );
}
