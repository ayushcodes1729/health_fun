/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  i64,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u32,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type HealthData = Account<HealthDataAccountData>;

export type HealthDataAccountData = {
  discriminator: Uint8Array;
  user: PublicKey;
  lastSyncTimestamp: bigint;
  epochDay: number;
  steps: number;
  sleepHours: number;
  gym: boolean;
  lastNonce: bigint;
};

export type HealthDataAccountDataArgs = {
  user: PublicKey;
  lastSyncTimestamp: number | bigint;
  epochDay: number;
  steps: number;
  sleepHours: number;
  gym: boolean;
  lastNonce: number | bigint;
};

export function getHealthDataAccountDataSerializer(): Serializer<
  HealthDataAccountDataArgs,
  HealthDataAccountData
> {
  return mapSerializer<HealthDataAccountDataArgs, any, HealthDataAccountData>(
    struct<HealthDataAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['user', publicKeySerializer()],
        ['lastSyncTimestamp', i64()],
        ['epochDay', u16()],
        ['steps', u32()],
        ['sleepHours', u8()],
        ['gym', bool()],
        ['lastNonce', u64()],
      ],
      { description: 'HealthDataAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([135, 173, 61, 234, 197, 214, 156, 146]),
    })
  ) as Serializer<HealthDataAccountDataArgs, HealthDataAccountData>;
}

export function deserializeHealthData(rawAccount: RpcAccount): HealthData {
  return deserializeAccount(rawAccount, getHealthDataAccountDataSerializer());
}

export async function fetchHealthData(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<HealthData> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'HealthData');
  return deserializeHealthData(maybeAccount);
}

export async function safeFetchHealthData(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<HealthData | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeHealthData(maybeAccount) : null;
}

export async function fetchAllHealthData(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<HealthData[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'HealthData');
    return deserializeHealthData(maybeAccount);
  });
}

export async function safeFetchAllHealthData(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<HealthData[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeHealthData(maybeAccount as RpcAccount));
}

export function getHealthDataGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'healthFun',
    'DADe3TShkZZnA8hvpMMUNmgGDe7LGwQbXZ2KDe2hTTXJ'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      user: PublicKey;
      lastSyncTimestamp: number | bigint;
      epochDay: number;
      steps: number;
      sleepHours: number;
      gym: boolean;
      lastNonce: number | bigint;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      user: [8, publicKeySerializer()],
      lastSyncTimestamp: [40, i64()],
      epochDay: [48, u16()],
      steps: [50, u32()],
      sleepHours: [54, u8()],
      gym: [55, bool()],
      lastNonce: [56, u64()],
    })
    .deserializeUsing<HealthData>((account) => deserializeHealthData(account))
    .whereField(
      'discriminator',
      new Uint8Array([135, 173, 61, 234, 197, 214, 156, 146])
    );
}
