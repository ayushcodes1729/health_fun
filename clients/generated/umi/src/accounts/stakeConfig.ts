/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  i64,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type StakeConfig = Account<StakeConfigAccountData>;

export type StakeConfigAccountData = {
  discriminator: Uint8Array;
  maxStake: bigint;
  maxFreezeTime: bigint;
  minFreezeTime: bigint;
  bump: number;
  verificationKey: PublicKey;
};

export type StakeConfigAccountDataArgs = {
  maxStake: number | bigint;
  maxFreezeTime: number | bigint;
  minFreezeTime: number | bigint;
  bump: number;
  verificationKey: PublicKey;
};

export function getStakeConfigAccountDataSerializer(): Serializer<
  StakeConfigAccountDataArgs,
  StakeConfigAccountData
> {
  return mapSerializer<StakeConfigAccountDataArgs, any, StakeConfigAccountData>(
    struct<StakeConfigAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['maxStake', u64()],
        ['maxFreezeTime', i64()],
        ['minFreezeTime', i64()],
        ['bump', u8()],
        ['verificationKey', publicKeySerializer()],
      ],
      { description: 'StakeConfigAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([238, 151, 43, 3, 11, 151, 63, 176]),
    })
  ) as Serializer<StakeConfigAccountDataArgs, StakeConfigAccountData>;
}

export function deserializeStakeConfig(rawAccount: RpcAccount): StakeConfig {
  return deserializeAccount(rawAccount, getStakeConfigAccountDataSerializer());
}

export async function fetchStakeConfig(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<StakeConfig> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'StakeConfig');
  return deserializeStakeConfig(maybeAccount);
}

export async function safeFetchStakeConfig(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<StakeConfig | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeStakeConfig(maybeAccount) : null;
}

export async function fetchAllStakeConfig(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<StakeConfig[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'StakeConfig');
    return deserializeStakeConfig(maybeAccount);
  });
}

export async function safeFetchAllStakeConfig(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<StakeConfig[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeStakeConfig(maybeAccount as RpcAccount));
}

export function getStakeConfigGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      maxStake: number | bigint;
      maxFreezeTime: number | bigint;
      minFreezeTime: number | bigint;
      bump: number;
      verificationKey: PublicKey;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      maxStake: [8, u64()],
      maxFreezeTime: [16, i64()],
      minFreezeTime: [24, i64()],
      bump: [32, u8()],
      verificationKey: [33, publicKeySerializer()],
    })
    .deserializeUsing<StakeConfig>((account) => deserializeStakeConfig(account))
    .whereField(
      'discriminator',
      new Uint8Array([238, 151, 43, 3, 11, 151, 63, 176])
    );
}
