/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type TreasuryConfig = Account<TreasuryConfigAccountData>;

export type TreasuryConfigAccountData = {
  discriminator: Uint8Array;
  mint: PublicKey;
  vault: PublicKey;
  bump: number;
  vaultBump: number;
};

export type TreasuryConfigAccountDataArgs = {
  mint: PublicKey;
  vault: PublicKey;
  bump: number;
  vaultBump: number;
};

export function getTreasuryConfigAccountDataSerializer(): Serializer<
  TreasuryConfigAccountDataArgs,
  TreasuryConfigAccountData
> {
  return mapSerializer<
    TreasuryConfigAccountDataArgs,
    any,
    TreasuryConfigAccountData
  >(
    struct<TreasuryConfigAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['mint', publicKeySerializer()],
        ['vault', publicKeySerializer()],
        ['bump', u8()],
        ['vaultBump', u8()],
      ],
      { description: 'TreasuryConfigAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([124, 54, 212, 227, 213, 189, 168, 41]),
    })
  ) as Serializer<TreasuryConfigAccountDataArgs, TreasuryConfigAccountData>;
}

export function deserializeTreasuryConfig(
  rawAccount: RpcAccount
): TreasuryConfig {
  return deserializeAccount(
    rawAccount,
    getTreasuryConfigAccountDataSerializer()
  );
}

export async function fetchTreasuryConfig(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<TreasuryConfig> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'TreasuryConfig');
  return deserializeTreasuryConfig(maybeAccount);
}

export async function safeFetchTreasuryConfig(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<TreasuryConfig | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeTreasuryConfig(maybeAccount) : null;
}

export async function fetchAllTreasuryConfig(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<TreasuryConfig[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'TreasuryConfig');
    return deserializeTreasuryConfig(maybeAccount);
  });
}

export async function safeFetchAllTreasuryConfig(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<TreasuryConfig[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) =>
      deserializeTreasuryConfig(maybeAccount as RpcAccount)
    );
}

export function getTreasuryConfigGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'healthFun',
    'AurTvci86g6Wx95U93zQtonzV3CwHq7Ehpw9chS1oqCa'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      mint: PublicKey;
      vault: PublicKey;
      bump: number;
      vaultBump: number;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      mint: [8, publicKeySerializer()],
      vault: [40, publicKeySerializer()],
      bump: [72, u8()],
      vaultBump: [73, u8()],
    })
    .deserializeUsing<TreasuryConfig>((account) =>
      deserializeTreasuryConfig(account)
    )
    .whereField(
      'discriminator',
      new Uint8Array([124, 54, 212, 227, 213, 189, 168, 41])
    );
}
